<html lang="en">
{{> head }}
  <body>
      {{> navbar}}
      {{> modal}}

    {{#if errorMessage}}
      <h1 class="error-message">{{errorMessage}}</h1>
    {{/if}}

    <div id="app">
        {{{body}}}
    </div>
    {{> footer }}
  </body>
</html>